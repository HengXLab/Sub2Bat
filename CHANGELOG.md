# 更新日志

本文件记录 Sub2Bat 每个发布版本中面向用户的主要变化。

## v1.1.3 - 2026-08-02

### Sub2API 兼容与导出

- 适配新版 Sub2API 的导出二次验证流程。服务器要求近期 TOTP 验证时，手动导出会显示验证码输入窗口；自动化导出会明确提示需要先在主界面完成验证。
- 兼容当前官方不携带数据库账号 ID 的备份导出格式，并按账号名称、平台、类型及官方跳过数量核验导出范围，避免误导出。
- 补充当前上游新增的平台和账号类型兼容项。

### 筛选、选择与测试

- 修复按分组、账号类型或状态筛选后，“全选所有筛选结果”无法正确选中完整范围的问题。
- 修复“未测试”和“最新测试”等筛选只在当前页或已测试账号中生效的问题；它们现在会在上方筛选范围内正确工作。
- 删除部分已选账号后，已选账号数量会同步更新。
- 完善测试结果分类：`401` 归为错误，`429` 归为限流，其他 HTTP 状态、超时和 EOF 归为连接异常，并提供状态说明对话框。

### 自动化与安全

- 修复自动执行停止后重新启动时的周期声明问题，新的调度可从第 1 轮安全开始。
- 自动化执行状态改为原子写入；状态损坏时会停止而不是冒险重复执行。
- 受保护的自动删除要求服务端支持强 `ETag` 条件删除。服务端不支持时会拒绝删除，避免账号状态变化后被误删。

### 界面、默认值与文档

- 主界面标题区域显示当前版本号。
- 新配置默认测试并发调整为 `20`，账号表格默认每页显示 `20` 条。
- 精简账号导出弹窗中的冗余提示。
- 补充项目来源、兼容性边界、界面预览及 CPA/Sub2API 转换功能的第三方引用说明。
